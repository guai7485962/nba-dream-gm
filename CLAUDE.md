# NBA Dream Team GM Claude Code rules

Read `README.md` before editing. The game source is `static/index.html`; generated
`dist/` is not committed. Preserve existing user changes in a dirty worktree.

For UI work, run `npm run ui:shot -- nba` from `C:\Users\User\claude_try`, then
inspect all screenshots and `artifacts/ui-lab/nba/report.json`. The preview URL is
`http://127.0.0.1:8000/`.
